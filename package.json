{
  "name": "live-sports-scoreboard",
  "private": true,
  "version": "1.0.0",
  "description": "Real-time football scoreboard — NestJS + Socket.io + React",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "dev": "npm run build:shared && concurrently -n server,web -c blue,green \"npm run start:dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build:shared && npm run build -w apps/server && npm run build -w apps/web",
    "test": "npm run build:shared && npm test -w apps/server && npm test -w apps/web",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write .",
    "og-image": "node scripts/generate-og-image.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.9.9",
    "sharp": "^0.34.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
